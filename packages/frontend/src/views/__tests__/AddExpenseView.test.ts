import { render, screen } from '@testing-library/vue'
import { userEvent } from '@testing-library/user-event'
import AddExpenseView from '../AddExpenseView.vue'

describe('AddExpenseView', () => {
  describe('when user fills out new expense details correctly and adds expense', () => {
    it('should show success message', () => {
      // render(AddExpenseView)
      // userEvent.type(screen.getByLabelText('Expense Date'), '2024-01-01')
    })
  })
})
